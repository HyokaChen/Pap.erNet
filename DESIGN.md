# Pap.er 桌面端应用设计规范

> **版本**: v1.0  
> **日期**: 2026-07-09  
> **平台**: macOS (Avalonia UI)  
> **主题**: 深色模式优先

---

## 目录

- [全局设计系统](#一全局设计系统)
- [首页 (Home)](#二首页-home)
- [我的壁纸 (My Wallpapers)](#三我的壁纸-my-wallpapers)
- [设置 (Settings)](#四设置-settings)
- [跨视图一致性](#五跨视图一致性)
- [数据模型参考](#六数据模型参考)

---

## 一、全局设计系统

### 1.1 色彩系统

#### 基础色板

| 颜色名称 | 色值 (Dark Mode) | 用途 |
|---------|------------------|------|
| **Background** | `#1E1E1E` | 面板整体背景 |
| **Surface** | `#2A2A2A` | Section 卡片背景、FAB 背景 |
| **Surface Elevated** | `#323232` | 按钮、开关轨道、输入框背景 |
| **Border** | `#3A3A3A` | 卡片边框、分割线 |
| **Border Light** | `#4A4A4A` | 激活状态边框、Hover 边框 |
| **Text Primary** | `#FFFFFF` | 主标题、正文、开关开启状态文字 |
| **Text Secondary** | `#A0A0A0` | 副标题、描述文字、禁用状态 |
| **Text Tertiary** | `#6E6E6E` | 占位符、版本号、Footer |
| **Accent** | `#4A90D9` | 开关开启状态、激活状态、强调色 |
| **Accent Hover** | `#5A9EE8` | Accent 悬停态 |
| **Danger** | `#FF6B6B` | 删除按钮 |
| **Overlay** | `rgba(0, 0, 0, 0.5)` | 图片暗色遮罩 |
| **Placeholder BG** | `#252525` | 图片加载前占位背景 |

#### 色彩层级

```
┌─────────────────────────────────────┐
│ Background: #1E1E1E                │  ← 最底层
│ ┌───────────────────────────────┐   │
│ │ Surface: #2A2A2A              │   │  ← Section 卡片
│ │ ┌─────────────────────────┐ │   │
│ │ │ Surface Elevated: #323232│ │   │  ← 按钮/开关/输入框
│ │ │                         │ │   │
│ │ └─────────────────────────┘ │   │
│ └───────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 1.2 字体系统

| 元素 | 颜色 | 字重 | 字号 |
|-----|------|------|------|
| 页面标题 | `#FFFFFF` | Semibold (600) | 18px |
| Section 标题 | `#FFFFFF` | Semibold (600) | 15px |
| Row 主文字 | `#FFFFFF` | Regular (400) | 14px |
| Row 副文字/描述 | `#A0A0A0` | Regular (400) | 13px |
| 占位符/版本号 | `#6E6E6E` | Regular (400) | 12px |
| Footer 链接 | `#A0A0A0` | Regular (400) | 13px |
| Footer Brand | `#6E6E6E` | Regular (400) | 12px |

### 1.3 间距系统

| 属性 | 数值 |
|-----|------|
| **面板宽度** | 350px (固定) |
| **面板最大高度** | 600px (超出可滚动) |
| **Section 圆角** | 12px |
| **Section 内边距** | 16px |
| **Section 间距** | 12px |
| **Row 高度** | 44px (标准) |
| **图标尺寸** | 20px × 20px |
| **图标容器** | 32px × 32px (圆角 8px) |

### 1.4 动画规范

| 动画 | 时长 | 曲线 |
|-----|------|------|
| Toggle 滑动 | 200ms | `cubic-bezier(0.4, 0, 0.2, 1)` |
| Dropdown 展开 | 150ms | `ease-out` |
| Button Press | 100ms | `ease-in-out` |
| Section 入场 | 300ms | `cubic-bezier(0, 0, 0.2, 1)` |
| 页面切换 | 250ms | `cubic-bezier(0.4, 0, 0.2, 1)` |
| 卡片 Hover 放大 | 200ms | `ease-out` |
| FAB Hover | 150ms | `ease` |
| 刷新旋转 | 1000ms | `linear` (infinite) |
| 图片加载淡入 | 300ms | `ease` |

### 1.5 通用组件规范

#### Toggle 开关

| 属性 | 数值 |
|-----|------|
| **轨道宽度** | 44px |
| **轨道高度** | 24px |
| **轨道圆角** | 12px (全圆) |
| **滑块直径** | 20px |
| **滑块圆角** | 10px (全圆) |
| **内边距** | 2px |

| 状态 | 轨道 (Track) | 滑块 (Thumb) |
|-----|-------------|-------------|
| **Off** | `#323232` | `#FFFFFF` |
| **On** | `#4A90D9` | `#FFFFFF` |
| **Off Hover** | `#3A3A3A` | `#FFFFFF` |
| **On Hover** | `#5A9EE8` | `#FFFFFF` |

#### Dropdown 下拉框

| 属性 | 数值 |
|-----|------|
| **背景** | `#323232` |
| **文字颜色** | `#FFFFFF` |
| **圆角** | 8px |
| **内边距** | 8px 12px |
| **最小宽度** | 100px |
| **箭头图标** | ChevronDown, 16px, `#A0A0A0` |
| **Hover 背景** | `#3A3A3A` |

#### Button 按钮

| 属性 | 数值 |
|-----|------|
| **背景** | `#323232` |
| **文字颜色** | `#FFFFFF` |
| **圆角** | 8px |
| **内边距** | 6px 14px |
| **字号** | 13px |
| **Hover 背景** | `#3A3A3A` |
| **Active 背景** | `#4A4A4A` |

#### Segmented Control 分段控制器

| 属性 | 数值 |
|-----|------|
| **容器背景** | `#323232` |
| **容器圆角** | 8px |
| **容器内边距** | 2px |
| **选项背景 (Active)** | `#4A90D9` |
| **选项文字 (Active)** | `#FFFFFF` |
| **选项文字 (Inactive)** | `#A0A0A0` |
| **选项圆角** | 6px |
| **选项内边距** | 6px 16px |
| **选项间距** | 2px |

---

## 二、首页 (Home)

### 2.1 视图结构

```
┌─────────────────────────────────────┐
│                                     │
│  p a p . e r              ○   ⚙   │  ← Header (镂空 Logo + 操作按钮)
│                                     │
│    发现      最新      竖屏          │  ← Tab 导航栏
│    ───                               │
│                                     │
│  ┌───────────────────────────────┐   │
│  │                               │   │
│  │      [壁纸图片 1]             │   │  ← 壁纸列表 (垂直滚动)
│  │                               │   │
│  └───────────────────────────────┘   │
│  ┌───────────────────────────────┐   │
│  │      [壁纸图片 2]             │   │
│  │                               │   │
│  └───────────────────────────────┘   │
│  ┌───────────────────────────────┐   │
│  │      [壁纸图片 3]             │   │
│  │                               │   │
│  └───────────────────────────────┘   │
│                                     │
│           ┌─────────┐               │
│           │   🔄    │               │  ← 常驻刷新按钮 (左下角)
│           └─────────┘               │
│                                     │
└─────────────────────────────────────┘
```

### 2.2 Header 区域

#### 布局

```
┌─────────────────────────────────────┐
│                                     │
│  p a p . e r              ○   ⚙   │
│  └─ Logo                  │   │    │
│                           │   │    │
│                    网格视图  设置   │
│                                     │
└─────────────────────────────────────┘
```

| 属性 | 规范 |
|-----|------|
| **Header 高度** | 56px |
| **背景** | 透明 (与毛玻璃背景融合) |
| **顶部内边距** | 16px |
| **左右内边距** | 16px |

#### 镂空 Logo 设计

| 属性 | 规范 |
|-----|------|
| **文字内容** | "pap.er" |
| **字体** | 系统无衬线字体 (SF Pro Display / Inter / PingFang SC) |
| **字号** | 24px |
| **字重** | Bold (700) |
| **字间距** | 0.1em |
| **文字填充** | `transparent` 或 `rgba(255,255,255,0.15)` |
| **文字描边** | `1px solid rgba(255,255,255,0.3)` |
| **文字阴影** | `0 1px 2px rgba(0,0,0,0.3)` |

#### 右侧操作按钮

| 按钮 | 图标 | 尺寸 | 颜色 | Hover 颜色 |
|-----|------|------|------|-----------|
| **网格视图切换** | Grid/Dots (九宫格/圆点) | 20px | `#A0A0A0` | `#FFFFFF` |
| **设置** | Gear/Settings | 20px | `#A0A0A0` | `#FFFFFF` |

| 属性 | 规范 |
|-----|------|
| **按钮尺寸** | 32px × 32px |
| **按钮间距** | 12px |
| **热区** | 32px × 32px |

### 2.3 Tab 导航栏

```
┌─────────────────────────────────────┐
│                                     │
│    发现        最新        竖屏      │
│    ───                               │
│   14px       14px       14px        │
│                                     │
└─────────────────────────────────────┘
```

| 属性 | 规范 |
|-----|------|
| **Tab 高度** | 40px |
| **Tab 间距** | 32px |
| **文字字号** | 14px |
| **文字字重** | Medium (500) |
| **未选中颜色** | `#A0A0A0` |
| **选中颜色** | `#FFFFFF` |
| **选中指示器** | 2px 底部下划线, `#FFFFFF` |
| **过渡动画** | 200ms, `ease-out` |

### 2.4 壁纸列表

#### 壁纸卡片布局

```
┌─────────────────────────────────────┐
│ ┌─────────────────────────────────┐ │
│ │                                 │ │
│ │      [壁纸图片]                 │ │
│ │                                 │ │
│ │ ┌─────────────────────────────┐ │ │
│ │ │ 🖥 设为桌面  ▼  [⬇️] [⭐]  │ │ │
│ │ └─────────────────────────────┘ │ │
│ │                                 │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

| 属性 | 规范 |
|-----|------|
| **卡片宽度** | 100% (填满容器, 左右 16px margin) |
| **图片高度** | 自适应 (保持原始比例, 最小 200px) |
| **卡片圆角** | 12px |
| **卡片间距** | 12px |
| **图片模式** | `object-fit: cover` |

#### 底部操作栏 (Toolbar)

| 属性 | 规范 |
|-----|------|
| **工具栏高度** | 40px |
| **工具栏背景** | `linear-gradient(transparent, rgba(0,0,0,0.6))` |
| **位置** | 图片底部 |
| **内边距** | 0 12px |

| 按钮 | 图标 | 尺寸 | 颜色 |
|-----|------|------|------|
| **设为桌面** | Monitor + ChevronDown | 16px | `#FFFFFF` |
| **下载** | Download | 20px | `#FFFFFF` |
| **收藏** | Star/Heart | 20px | `#FFFFFF` |

### 2.5 常驻刷新按钮 (FAB)

#### 位置

```
┌─────────────────────────────────────┐
│                                     │
│   [壁纸列表...]                     │
│                                     │
│                                     │
│                                     │
│                                     │
│   ┌─────┐                           │
│   │  🔄  │                           │  ← 左下角固定
│   └─────┘                           │
│                                     │
└─────────────────────────────────────┘
```

#### 设计规格

| 属性 | 规范 |
|-----|------|
| **按钮尺寸** | 48px × 48px |
| **按钮形状** | 圆形 (border-radius: 50%) |
| **背景颜色** | `rgba(42, 42, 42, 0.9)` |
| **边框** | 1px solid `#3A3A3A` |
| **图标** | Refresh/Reload (循环箭头) |
| **图标尺寸** | 20px |
| **图标颜色** | `#FFFFFF` |
| **阴影** | `0 4px 12px rgba(0,0,0,0.3)` |
| **左边距** | 16px |
| **底边距** | 16px |
| **定位** | Fixed / Absolute (相对于视口) |
| **z-index** | 最高层级 |

#### 交互状态

| 状态 | 效果 |
|-----|------|
| **默认** | 静态显示 |
| **Hover** | 背景 `#3A3A3A`, scale(1.05) |
| **Active/Press** | scale(0.95), 背景 `#4A4A4A` |
| **刷新中** | 图标旋转动画 (360deg, 1s, linear, infinite) |
| **成功** | 图标变为 Checkmark, 短暂显示后恢复 |

---

## 三、我的壁纸 (My Wallpapers)

### 3.1 视图结构

```
┌─────────────────────────────────────┐
│  ┌───┐  我的壁纸 (21)        🗂    │  ← Navigation Bar
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │     当前随机壁纸                 │  │
│  │           [OFF]                │  │
│  │  ┌─────┐    ┌─────┐            │  │
│  │  │ 📷  │    │ 📷  │            │  │  ← 双屏预览占位
│  │  └─────┘    └─────┘            │  │
│  │   ⬇️          ⬇️               │  │  ← 下载按钮
│  └───────────────────────────────┘  │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │ ┌─────────────────────────┐   │  │
│  │ │ 🌅 日落海滩壁纸            │   │  │  ← 壁纸卡片 1
│  │ │ [4K] 设为桌面 ▼  [🗑️]   │   │  │
│  │ └─────────────────────────┘   │  │
│  │ ┌─────────────────────────┐   │  │
│  │ │ ✈️ 飞机降落壁纸            │   │  │  ← 壁纸卡片 2
│  │ │                         │   │  │
│  │ └─────────────────────────┘   │  │
│  │ ┌─────────────────────────┐   │  │
│  │ │ 🍑 桃子特写壁纸            │   │  │  ← 壁纸卡片 3
│  │ │                         │   │  │
│  │ └─────────────────────────┘   │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### 3.2 Navigation Bar

| 属性 | 规范 |
|-----|------|
| **高度** | 48px |
| **背景** | 透明 |
| **返回按钮** | ChevronLeft, 24px, `#FFFFFF` |
| **标题** | 18px, Semibold, `#FFFFFF` |
| **标题格式** | `我的壁纸 (21)` — 括号内为壁纸数量 |
| **文件夹按钮** | Folder, 20px, `#A0A0A0` |
| **按钮热区** | 44px × 44px |

### 3.3 随机壁纸控制区

#### 整体结构

```
┌─────────────────────────────────────┐
│                                     │
│         当前随机壁纸                  │
│           [状态标签]                 │
│                                     │
│    ┌─────┐      ┌─────┐            │
│    │ 左屏 │      │ 右屏 │            │
│    │ 预览 │      │ 预览 │            │
│    └─────┘      └─────┘            │
│      ⬇️           ⬇️               │
│                                     │
└─────────────────────────────────────┘
```

#### 状态标签 (Status Badge)

| 属性 | OFF 状态 | ON 状态 |
|-----|---------|---------|
| **文字** | "OFF" | "ON" |
| **背景** | `#323232` | `#4A90D9` |
| **文字颜色** | `#6E6E6E` | `#FFFFFF` |
| **字号** | 12px |
| **圆角** | 4px |
| **内边距** | 2px 8px |

#### 预览框 (Preview Frame)

| 属性 | 规范 |
|-----|------|
| **尺寸** | 120px × 80px |
| **圆角** | 8px |
| **背景** | `#252525` (占位) |
| **边框** | 2px solid `#3A3A3A` |
| **边框 (激活)** | 2px solid `#4A90D9` |
| **图片模式** | `object-fit: cover` |
| **占位图标** | Image icon, 24px, `#6E6E6E` |

#### 下载按钮

| 属性 | 规范 |
|-----|------|
| **尺寸** | 24px × 24px |
| **图标** | Download/ArrowDown, 16px |
| **颜色** | `#A0A0A0` |
| **Hover 颜色** | `#FFFFFF` |
| **位置** | 预览框正下方居中 |

### 3.4 壁纸卡片

#### 整体结构

```
┌─────────────────────────────────────┐
│ ┌─────────────────────────────────┐ │
│ │ ┌─────┐                   ┌───┐ │ │
│ │ │ 4K  │                   │ 🗑️│ │ │
│ │ └─────┘                   └───┘ │ │
│ │                                 │ │
│ │         [壁纸图片内容]           │ │
│ │                                 │ │
│ │ ┌─────────────────────────────┐ │ │
│ │ │ 🖥  设为桌面        ▼        │ │ │
│ │ └─────────────────────────────┘ │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

#### 4K 标签 (Quality Badge)

| 属性 | 规范 |
|-----|------|
| **文字** | "4K" |
| **背景** | `rgba(255, 255, 255, 0.9)` |
| **文字颜色** | `#1E1E1E` |
| **字号** | 11px |
| **字重** | Bold (700) |
| **圆角** | 4px |
| **内边距** | 2px 6px |
| **位置** | 图片左上角, 边距 8px |

#### 删除按钮

| 属性 | 规范 |
|-----|------|
| **尺寸** | 32px × 32px |
| **图标** | Trash, 16px |
| **背景** | `rgba(30, 30, 30, 0.6)` |
| **图标颜色** | `#FFFFFF` |
| **圆角** | 6px |
| **位置** | 图片右上角, 边距 8px |
| **Hover 背景** | `rgba(255, 107, 107, 0.9)` |

#### 底部工具栏

| 属性 | 规范 |
|-----|------|
| **高度** | 40px |
| **背景** | `rgba(30, 30, 30, 0.85)` |
| **Backdrop Filter** | `blur(12px)` |
| **圆角 (底部)** | 12px |
| **内边距** | 0 12px |

##### 设为桌面按钮

| 属性 | 规范 |
|-----|------|
| **图标** | Monitor/Desktop, 16px |
| **文字** | "设为桌面" |
| **文字颜色** | `#FFFFFF` |
| **字号** | 13px |
| **下拉箭头** | ChevronDown, 12px |

##### 下拉菜单

```
┌─────────────────┐
│ 设为桌面         │  ← 当前选中
├─────────────────┤
│ 设为锁屏         │
├─────────────────┤
│ 同时设置         │
└─────────────────┘
```

| 属性 | 规范 |
|-----|------|
| **背景** | `#2A2A2A` |
| **边框** | 1px solid `#3A3A3A` |
| **圆角** | 8px |
| **阴影** | `0 4px 12px rgba(0,0,0,0.4)` |
| **选项高度** | 36px |
| **选项内边距** | 0 12px |
| **Hover 背景** | `#3A3A3A` |

### 3.5 空状态

```
┌─────────────────────────────────────┐
│                                     │
│              📷                     │
│                                     │
│         暂无壁纸                     │
│    点击右上角文件夹导入壁纸           │
│                                     │
└─────────────────────────────────────┘
```

| 属性 | 规范 |
|-----|------|
| **图标** | Image/Photo, 48px, `#6E6E6E` |
| **标题** | 16px, `#A0A0A0` |
| **描述** | 13px, `#6E6E6E` |
| **垂直居中** | 是 |

---

## 四、设置 (Settings)

### 4.1 视图结构

```
┌─────────────────────────────────────┐
│  ┌───┐   pap.er              ⏻    │  ← Navigation Bar
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐   │
│  │ 🎲 随机壁纸                    │   │  ← Section 1
│  │ ┌─────────────────────────┐ │   │
│  │ │ 开启自动换壁纸      [○]  │ │   │  ← Toggle Row
│  │ │ 间隔            [每天 ▼]  │ │   │  ← Dropdown Row
│  │ │ 来源      [在线] [壁纸库]  │ │   │  ← Segmented Control Row
│  │ │ 最近    [预览] [预览]      │ │   │  ← Thumbnail Row
│  │ │ 主屏幕与外接屏幕相同壁纸 [○]│ │   │  ← Toggle Row
│  │ └─────────────────────────┘ │   │
│  └───────────────────────────────┘   │
│  ┌───────────────────────────────┐   │
│  │ 🖼 壁纸库                       │   │  ← Section 2
│  │ ┌─────────────────────────┐ │   │
│  │ │ 壁纸库位置          [选择] │ │   │  ← Button Row
│  │ │ ┌─────────────────────┐ │ │   │
│  │ │ │ 📁 /Users/...       │ │ │   │  ← Info Card
│  │ │ │ 壁纸库占用     37.84MB │ │ │   │
│  │ │ └─────────────────────┘ │ │   │
│  │ │ 自动导入目录内图片    [●] │ │   │  ← Toggle Row (On)
│  │ └─────────────────────────┘ │   │
│  └───────────────────────────────┘   │
│  ┌───────────────────────────────┐   │
│  │ 🖥 个性化选项                    │   │  ← Section 3
│  │ ┌─────────────────────────┐ │   │
│  │ │ 所有桌面空间使用相同壁纸 [○]│ │   │  ← Toggle Row
│  │ │ 将菜单栏调暗          [○] │ │   │  ← Toggle Row
│  │ └─────────────────────────┘ │   │
│  └───────────────────────────────┘   │
│  ┌───────────────────────────────┐   │
│  │ ⚙️ 通用                         │   │  ← Section 4
│  │ ┌─────────────────────────┐ │   │
│  │ │ 语言            [简中 ▼] │ │   │  ← Dropdown Row
│  │ │ 随系统启动           [●] │ │   │  ← Toggle Row (On)
│  │ │ 在程序坞显示图标     [●] │ │   │  ← Toggle Row (On)
│  │ │ v5.4.5         [新版变化] │ │   │  ← Version Row
│  │ └─────────────────────────┘ │   │
│  └───────────────────────────────┘   │
│         关于我们 · 退出 pap.er        │  ← Footer Links
│         pap.er 优雅壁纸              │  ← Footer Brand
└─────────────────────────────────────┘
```

### 4.2 Navigation Bar

| 属性 | 规范 |
|-----|------|
| **高度** | 48px |
| **背景** | 透明 |
| **返回按钮** | ChevronLeft, 24px, `#FFFFFF` |
| **标题** | 18px, Semibold, `#FFFFFF`, 居中 |
| **退出按钮** | Power, 20px, `#A0A0A0` |
| **按钮热区** | 44px × 44px |

### 4.3 Section 组件

| 属性 | 规范 |
|-----|------|
| **背景** | `#2A2A2A` |
| **圆角** | 12px |
| **内边距** | 16px |
| **Header 底部间距** | 12px |
| **Row 间距** | 0px (相邻行紧贴，无分隔线) |

#### Section Header

| 属性 | 规范 |
|-----|------|
| **图标容器** | 32px × 32px, 圆角 8px, 背景 `#323232` |
| **图标** | SF Symbols / 自定义图标, 20px, 颜色 `#A0A0A0` |
| **标题** | 15px, Semibold, `#FFFFFF` |
| **图标与标题间距** | 12px |

### 4.4 行类型布局

#### 类型 A: Toggle Row

```
┌─────────────────────────────────────┐
│  开启自动换壁纸              [○────] │
└─────────────────────────────────────┘
```

#### 类型 B: Dropdown Row

```
┌─────────────────────────────────────┐
│  间隔                 [每天    ▼]   │
└─────────────────────────────────────┘
```

#### 类型 C: Segmented Control Row

```
┌─────────────────────────────────────┐
│  来源              [在线] [壁纸库]   │
└─────────────────────────────────────┘
```

#### 类型 D: Button Row

```
┌─────────────────────────────────────┐
│  壁纸库位置                  [选择]  │
└─────────────────────────────────────┘
```

#### 类型 E: Info Card Row

```
┌─────────────────────────────────────┐
│  ┌───────────────────────────────┐  │
│  │ 📁 /Users/krp/Pictures/pap.er │  │
│  │ 壁纸库占用           37.84 MB │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

| 属性 | 规范 |
|-----|------|
| **背景** | `#323232` |
| **圆角** | 8px |
| **内边距** | 12px |
| **路径文字** | 14px, `#FFFFFF` |
| **描述文字** | 13px, `#A0A0A0` |
| **数值文字** | 13px, `#FFFFFF` |

#### 类型 F: Thumbnail Row

```
┌─────────────────────────────────────┐
│  最近                               │
│  ┌─────────┐  ┌─────────┐          │
│  │         │  │         │          │
│  │  预览   │  │  预览   │          │
│  │         │  │         │          │
│  └─────────┘  └─────────┘          │
└─────────────────────────────────────┘
```

| 属性 | 规范 |
|-----|------|
| **尺寸** | 80px × 60px |
| **圆角** | 8px |
| **背景** | `#323232` |
| **占位文字** | 12px, `#6E6E6E` |
| **图片模式** | `object-fit: cover` |
| **间距** | 8px |

### 4.5 Footer 区域

```
┌─────────────────────────────────────┐
│                                     │
│      关于我们  ·  退出 pap.er        │
│         pap.er 优雅壁纸              │
│                                     │
└─────────────────────────────────────┘
```

| 元素 | 规范 |
|-----|------|
| **链接文字** | 13px, `#A0A0A0` |
| **链接 Hover** | `#FFFFFF`, 下划线 |
| **分隔符** | `·`, 13px, `#6E6E6E` |
| **品牌文字** | 12px, `#6E6E6E` |
| **顶部间距** | 20px |
| **链接间距** | 16px |

---

## 五、跨视图一致性

### 5.1 设计一致性检查表

| 维度 | 首页 | 我的壁纸 | 设置 | 一致性 |
|-----|------|---------|------|--------|
| **面板宽度** | 350px | 350px | 350px | ✅ 一致 |
| **背景色** | `#1E1E1E` | `#1E1E1E` | `#1E1E1E` | ✅ 一致 |
| **Surface 色** | `#2A2A2A` | `#2A2A2A` | `#2A2A2A` | ✅ 一致 |
| **Elevated 色** | `#323232` | `#323232` | `#323232` | ✅ 一致 |
| **主文字色** | `#FFFFFF` | `#FFFFFF` | `#FFFFFF` | ✅ 一致 |
| **副文字色** | `#A0A0A0` | `#A0A0A0` | `#A0A0A0` | ✅ 一致 |
| **Accent 色** | `#4A90D9` | `#4A90D9` | `#4A90D9` | ✅ 一致 |
| **圆角系统** | 12px/8px | 12px/8px | 12px/8px | ✅ 一致 |
| **Navigation Bar** | 48px | 48px | 48px | ✅ 一致 |
| **Section 间距** | 12px | 12px | 12px | ✅ 一致 |

### 5.2 交互模式一致性

| 交互 | 首页 | 我的壁纸 | 设置 |
|-----|------|---------|------|
| **滚动** | 垂直, 隐藏滚动条 | 垂直, 隐藏滚动条 | 垂直, 隐藏滚动条 |
| **返回按钮** | - | 有 (左上角) | 有 (左上角) |
| **设置入口** | Header 右上角 | - | - |
| **图片加载** | 淡入动画 | 淡入动画 | - |
| **按钮反馈** | Scale + 颜色变化 | Scale + 颜色变化 | Scale + 颜色变化 |

---

## 六、数据模型参考

### 6.1 Settings State

```typescript
interface SettingsState {
  // 随机壁纸
  autoChangeWallpaper: boolean;
  interval: 'daily' | 'weekly' | 'monthly' | 'hourly';
  source: 'online' | 'local';
  mirrorDisplays: boolean;

  // 壁纸库
  wallpaperPath: string;
  autoImport: boolean;

  // 个性化
  sameWallpaperAllSpaces: boolean;
  dimMenuBar: boolean;

  // 通用
  language: 'zh-CN' | 'en' | 'ja' | '...';
  launchAtLogin: boolean;
  showInDock: boolean;

  // 元数据
  version: string;
}
```

### 6.2 Wallpaper Model

```typescript
interface Wallpaper {
  id: string;
  url: string;
  thumbnailUrl: string;
  title: string;
  resolution: string;      // e.g., "3840x2160"
  is4K: boolean;
  fileSize: number;        // in bytes
  createdAt: Date;
  source: 'online' | 'local';
}
```

### 6.3 My Wallpapers State

```typescript
interface MyWallpapersState {
  wallpapers: Wallpaper[];
  totalCount: number;
  selectedWallpaperId: string | null;
  randomWallpaperEnabled: boolean;
  currentRandomWallpapers: {
    primary: Wallpaper | null;
    secondary: Wallpaper | null;
  };
}
```

---

## 附录: 设计原则

1. **层级清晰**: 通过 Background → Surface → Elevated 三层背景建立视觉层级
2. **间距一致**: 12px/16px 的倍数系统，保持呼吸感
3. **圆角统一**: 12px (Section), 8px (组件), 全圆 (Toggle/FAB)
4. **色彩克制**: 仅使用 Accent 蓝色作为强调色，其余为灰度
5. **交互反馈**: 每个可交互元素都有明确的 Hover/Active 状态
6. **macOS 原生感**: 毛玻璃、圆角、隐藏滚动条、深色模式优先
7. **图片优先**: 壁纸是主角，UI 元素尽量克制不喧宾夺主
8. **镂空品牌**: Logo 利用毛玻璃特性，形成独特的品牌视觉标识
